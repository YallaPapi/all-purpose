# all_purpose_pattern

Task management application from PRD

## Overview

This agent was automatically generated by the Scaffold Generator on 2025-07-27 15:20:14 based on PRD-Parser output.

## Features

### Planned Implementation Tasks

- **Setup Node.js Project with AST Parsing** (Priority: high)
  Initialize the Node.js project structure and integrate AST parsing libraries for code analysis capabilities.
  
  *Details:* 1. Create a new Node.js project with TypeScript support
2. Install required dependencies:
   - @babel/parser for AST generation
   - @babel/traverse for AST traversal
   - @babel/types for AST manipulation
   - handlebars for template generation
3. Configure TypeScript with appropriate settings
4. Set up project directory structure:
   &#x60;&#x60;&#x60;
   /src
     /analyzers - Code analysis modules
     /transformers - Code transformation modules
     /templates - Handlebars templates
     /validators - Validation system
     /utils - Helper utilities
   /tests - Test files
   &#x60;&#x60;&#x60;
5. Create basic entry point that exposes the core API
6. Set up linting and formatting with ESLint and Prettier
7. Configure Jest for testing

- **Implement Hardcoded Pattern Detection System** (Priority: high)
  Create a system that analyzes code to detect hardcoded elements including industry lists, location restrictions, business type limits, and other anti-patterns.
  
  *Details:* 1. Develop AST-based analyzers for different types of hardcoded patterns:
   &#x60;&#x60;&#x60;typescript
   // Industry list detector
   function detectIndustryLists(ast: AST): HardcodedPattern[] {
     const patterns: HardcodedPattern[] &#x3D; [];
     traverse(ast, {
       ArrayExpression(path) {
         // Check if array contains string literals that look like industries
         // Use NLP or keyword matching to identify potential industry lists
       }
     });
     return patterns;
   }
   &#x60;&#x60;&#x60;

2. Create detectors for:
   - Hardcoded limitation constants (e.g., &#x60;const maxIndustries &#x3D; 50&#x60;)
   - Conditional logic based on hardcoded values (e.g., &#x60;if (industry &#x3D;&#x3D;&#x3D; &#x27;automotive&#x27;)&#x60;)
   - Hardcoded API endpoints or configuration
   - Hardcoded UI text and messaging

3. Implement a pattern registry to categorize and prioritize detected patterns
4. Create a scoring system to evaluate the severity of hardcoded limitations
5. Build a reporting mechanism to summarize detected patterns

- **Build Universal Template Generation System** (Priority: high)
  Develop a Handlebars-based template generation system that can create universal, configuration-driven code patterns to replace hardcoded elements.
  
  *Details:* 1. Create a template manager class:
   &#x60;&#x60;&#x60;typescript
   class TemplateManager {
     private templates: Map&lt;string, HandlebarsTemplateDelegate&gt;;
     
     constructor() {
       this.templates &#x3D; new Map();
       this.loadBuiltInTemplates();
     }
     
     loadBuiltInTemplates() {
       // Load templates for common patterns
       // - Dynamic industry handling
       // - Location-agnostic code
       // - Configuration-driven business logic
     }
     
     registerTemplate(name: string, template: string) {
       this.templates.set(name, Handlebars.compile(template));
     }
     
     generate(templateName: string, context: any): string {
       const template &#x3D; this.templates.get(templateName);
       if (!template) throw new Error(&#x60;Template not found: ${templateName}&#x60;);
       return template(context);
     }
   }
   &#x60;&#x60;&#x60;

2. Create templates for:
   - Dynamic prompt templates (similar to lib/prompt-template-manager.ts)
   - Universal API parameter handling
   - Industry-agnostic UI components
   - Configuration-driven behavior systems
   - Fallback and validation patterns

3. Implement helper functions for template context generation
4. Build template composition system for complex transformations
5. Create documentation generator for created templates

- **Implement Code Transformation Engine** (Priority: high)
  Create a system that can transform hardcoded code into dynamic, configuration-driven code using the detected patterns and template system.
  
  *Details:* 1. Develop AST transformation utilities:
   &#x60;&#x60;&#x60;typescript
   function transformHardcodedArray(path: NodePath, templateManager: TemplateManager): void {
     // Replace hardcoded array with dynamic configuration
     const configName &#x3D; inferConfigurationName(path);
     const replacement &#x3D; templateManager.generate(&#x27;dynamicArray&#x27;, { configName });
     path.replaceWithSourceString(replacement);
   }
   &#x60;&#x60;&#x60;

2. Create transformers for each pattern type:
   - Industry list transformers
   - Location restriction removers
   - Business type limitation eliminators
   - Hardcoded constant replacers
   - Conditional logic transformers

3. Implement transformation context to maintain state during transformations
4. Build source map generation for traceability
5. Create transformation pipeline to apply multiple transformations
6. Implement code formatting for transformed code

- **Build Validation System for Unlimited Scope** (Priority: medium)
  Create a validation system that ensures no hardcoded limitations remain in the transformed code and verifies unlimited scalability.
  
  *Details:* 1. Implement validation rules:
   &#x60;&#x60;&#x60;typescript
   const validationRules &#x3D; [
     {
       name: &#x27;no-hardcoded-industries&#x27;,
       validate: (ast: AST) &#x3D;&gt; {
         // Check for any remaining hardcoded industry references
         return { passed: boolean, violations: Violation[] };
       }
     },
     // Additional rules for locations, business types, etc.
   ];
   &#x60;&#x60;&#x60;

2. Create validators for:
   - No hardcoded industry lists
   - No location limitations
   - No business type restrictions
   - Unlimited scalability
   - Configuration-driven behavior

3. Implement validation reporting with detailed violation information
4. Build remediation suggestions for validation failures
5. Create validation summary with confidence scores
6. Implement continuous validation during transformation process

- **Implement Context7 Integration** (Priority: medium)
  Integrate with Context7 to ensure generated code follows current best practices and documentation patterns.
  
  *Details:* 1. Create Context7 client for accessing current implementation patterns:
   &#x60;&#x60;&#x60;typescript
   class Context7Client {
     async getCurrentPatterns(domain: string): Promise&lt;Pattern[]&gt; {
       // Fetch current implementation patterns from Context7
     }
     
     async enhanceCode(code: string, context: string): Promise&lt;string&gt; {
       // Use Context7 to enhance generated code with best practices
     }
     
     async generateDocumentation(code: string): Promise&lt;Documentation&gt; {
       // Generate documentation using Context7 patterns
     }
   }
   &#x60;&#x60;&#x60;

2. Implement pattern synchronization with Context7
3. Create code enhancement pipeline using Context7 insights
4. Build documentation generation using Context7 templates
5. Implement best practice validation against Context7 standards
6. Create feedback loop for continuous improvement

- **Implement Agent-Driven Development Integration** (Priority: medium)
  Integrate the All-Purpose Pattern methodology from agent_driven_development_methodology.md and implement the 5-document framework generation.
  
  *Details:* 1. Parse and implement the All-Purpose Pattern methodology:
   &#x60;&#x60;&#x60;typescript
   class AgentDrivenDevelopment {
     applyAllPurposePattern(codebase: Codebase): TransformationPlan {
       // Apply methodology from agent_driven_development_methodology.md
     }
     
     generate5DocumentFramework(system: System): Documentation[] {
       // Generate the 5-document framework
       return [
         this.generateOverviewDoc(system),
         this.generateArchitectureDoc(system),
         this.generateImplementationDoc(system),
         this.generateTestingDoc(system),
         this.generateDeploymentDoc(system)
       ];
     }
     
     apply30MinuteRule(problem: Problem): Solution[] {
       // Implement the 30-Minute Rule for debugging and alternatives
     }
   }
   &#x60;&#x60;&#x60;

2. Implement the 5-document framework generator
3. Create 30-Minute Rule implementation for debugging
4. Build Vercel-native architecture pattern detection and application
5. Implement methodology validation to ensure compliance
6. Create agent-driven development workflow integration

- **Implement Meta-Agent Integration** (Priority: medium)
  Create integration points with existing PRD-Parser, Scaffold-Generator, and TaskMaster agents following the meta-agent factory architecture.
  
  *Details:* 1. Implement meta-agent factory architecture:
   &#x60;&#x60;&#x60;typescript
   class MetaAgentFactory {
     createAllPurposePatternAgent(config: AgentConfig): Agent {
       return new AllPurposePatternAgent(config);
     }
     
     integrateWithPRDParser(prdParser: PRDParser): IntegratedAgent {
       // Integrate with PRD-Parser
     }
     
     integrateWithScaffoldGenerator(scaffoldGenerator: ScaffoldGenerator): IntegratedAgent {
       // Integrate with Scaffold-Generator
     }
     
     integrateWithTaskMaster(taskMaster: TaskMaster): IntegratedAgent {
       // Integrate with TaskMaster
     }
   }
   &#x60;&#x60;&#x60;

2. Create integration adapters for each agent type
3. Implement communication protocols between agents
4. Build shared context management for multi-agent workflows
5. Create agent orchestration system
6. Implement result aggregation and conflict resolution

- **Implement Performance Optimization** (Priority: low)
  Optimize the agent to efficiently analyze large codebases, transform systems quickly, and support concurrent transformations.
  
  *Details:* 1. Implement incremental analysis for large codebases:
   &#x60;&#x60;&#x60;typescript
   class IncrementalAnalyzer {
     private analyzed: Set&lt;string&gt; &#x3D; new Set();
     private results: Map&lt;string, AnalysisResult&gt; &#x3D; new Map();
     
     async analyzeFile(filePath: string): Promise&lt;AnalysisResult&gt; {
       if (this.analyzed.has(filePath)) {
         return this.results.get(filePath)!;
       }
       
       // Perform analysis
       const result &#x3D; await this.performAnalysis(filePath);
       
       this.analyzed.add(filePath);
       this.results.set(filePath, result);
       
       return result;
     }
     
     private async performAnalysis(filePath: string): Promise&lt;AnalysisResult&gt; {
       // Optimized analysis implementation
     }
   }
   &#x60;&#x60;&#x60;

2. Implement parallel processing for file analysis
3. Create caching mechanisms for parsed ASTs and templates
4. Optimize template rendering for performance
5. Implement worker pool for concurrent transformations
6. Create progress tracking and estimation system
7. Implement memory-efficient processing for large files

- **Create End-to-End Testing and Documentation** (Priority: low)
  Develop comprehensive end-to-end tests and documentation for the All-Purpose Pattern Agent.
  
  *Details:* 1. Create end-to-end test suite:
   &#x60;&#x60;&#x60;typescript
   describe(&#x27;All-Purpose Pattern Agent E2E&#x27;, () &#x3D;&gt; {
     it(&#x27;should transform a hardcoded system into a universal system&#x27;, async () &#x3D;&gt; {
       // Setup test codebase with hardcoded limitations
       const testCodebase &#x3D; setupTestCodebase();
       
       // Run the agent
       const agent &#x3D; new AllPurposePatternAgent();
       const result &#x3D; await agent.transform(testCodebase);
       
       // Validate the result
       expect(result.validationResults.passed).toBe(true);
       expect(result.transformedFiles.length).toBeGreaterThan(0);
       
       // Verify no hardcoded limitations remain
       const validator &#x3D; new UnlimitedScopeValidator();
       const validationResult &#x3D; await validator.validate(result.transformedCodebase);
       expect(validationResult.passed).toBe(true);
     });
   });
   &#x60;&#x60;&#x60;

2. Create sample hardcoded systems for testing
3. Implement validation tests for transformed systems
4. Create performance benchmarking tests
5. Write comprehensive documentation:
   - API reference
   - Usage examples
   - Pattern detection guide
   - Template creation guide
   - Integration guide
6. Create tutorial for extending the agent with new patterns


## Installation

```bash
npm install
```

## Configuration

Configuration is handled through the `config/default.json` file and environment variables.

### Environment Variables

- `LOG_LEVEL` - Set logging level (debug, info, warn, error)
- `TIMEOUT` - Set operation timeout in milliseconds
- `NODE_ENV` - Set environment (development, production)

### Config File

See `config/default.json` for detailed configuration options.

## Usage

### Programmatic Usage

```javascript
const { All_purpose_patternAgent } = require('./main');

async function example() {
  const agent = new All_purpose_patternAgent({
    logLevel: 'info',
    timeout: 30000
  });
  
  await agent.initialize();
  const result = await agent.process({ 
    // Your input data here
  });
  
  console.log('Result:', result);
  await agent.cleanup();
}

example().catch(console.error);
```

### Direct Execution

```bash
node main.js
```

### Using the main function

```javascript
const { main } = require('./main');

main({
  input: { /* your data */ },
  logLevel: 'info'
}).then(result => {
  console.log('Success:', result);
}).catch(error => {
  console.error('Error:', error);
});
```

## API Reference

### All_purpose_patternAgent Class

#### Constructor

```javascript
new All_purpose_patternAgent(config)
```

- `config` (Object) - Configuration options
  - `logLevel` (string) - Logging level (default: 'info')
  - `timeout` (number) - Operation timeout in ms (default: 30000)

#### Methods

##### `initialize()`

Initialize the agent and prepare for processing.

**Returns:** `Promise<void>`

##### `process(input)`

Process the provided input data.

**Parameters:**
- `input` (Object) - Data to process

**Returns:** `Promise<Object>` - Processing result

##### `cleanup()`

Clean up resources and connections.

**Returns:** `Promise<void>`

##### `getStatus()`

Get current agent status information.

**Returns:** `Object` - Status information

## Development

### Project Structure

```
allpurposepattern/
├── main.js              # Main agent implementation
├── config/
│   └── default.json     # Configuration file
├── templates/           # Template files (if applicable)
├── tests/               # Test files
└── README.md           # This file
```

### Testing

```bash
npm test
```

### Linting

```bash
npm run lint
```

## Error Handling

The agent implements comprehensive error handling:

- All operations are wrapped in try-catch blocks
- Errors are logged with appropriate severity levels
- Failed operations throw descriptive error messages
- Cleanup is performed even when errors occur

## Logging

The agent uses `chalk` for colored console output:

- 🚀 Blue: Initialization and process start messages
- ✅ Green: Success messages
- ❌ Red: Error messages
- 🔄 Blue: Processing status updates
- 🧹 Blue: Cleanup messages

## Contributing

1. Follow the existing code style and patterns
2. Add tests for new functionality
3. Update documentation as needed
4. Ensure all tests pass before submitting changes

## License

ISC

---

*Generated by Scaffold Generator on 2025-07-27 15:20:14*