# Prospector

Agent for prospector functionality

## Overview

This agent was automatically generated by the Scaffold Generator on 2025-07-25 11:31:02 based on PRD-Parser output.

## Features

### Planned Implementation Tasks

- **Initialize Prospector Agent Project Structure** (Priority: high)
  Set up the foundational Node.js project structure with TypeScript, environment configuration, and core dependencies.
  
  *Details:* Create package.json with Node.js/TypeScript setup. Install dependencies: express, redis, google-maps-api, dotenv, jest. Set up TypeScript config, ESLint, and basic project structure with src/, tests/, and config/ directories.

- **Create Configuration Management System** (Priority: high)
  Implement the All-Purpose Pattern configuration system that accepts ANY industry/location combination without hardcoded limitations.
  
  *Details:* Create ProspectorConfig interface and validation. Support dynamic industry queries, location parameters, custom search terms, and business types. Use environment variables and JSON configuration files.

- **Implement Google Places API Integration** (Priority: high)
  Build the core Google Places API (New) integration with Text Search and Nearby Search endpoints.
  
  *Details:* Implement Google Places API client with proper authentication. Support Text Search for keyword-based discovery and Nearby Search for location-based discovery. Handle API responses and error cases.

- **Build Dynamic Query Generation Engine** (Priority: high)
  Create intelligent query generation that adapts to any industry and location without limitations.
  
  *Details:* Implement query builder that constructs search terms from industry keywords, location data, and business categories. Support multiple query strategies and optimization based on results.

- **Implement Redis Lead Deduplication System** (Priority: high)
  Build Redis-based deduplication to prevent storing duplicate leads and optimize storage.
  
  *Details:* Use Redis sets/hashes to track discovered leads by phone, email, or business name. Implement fast lookup and duplicate checking before storage.

- **Create Universal Output Manager** (Priority: medium)
  Standardize all discovered leads into the ProspectedLead output format and prepare for downstream integration.
  
  *Details:* Implement the ProspectedLead TypeScript interface. Map all source data into this format. Ensure all required fields are populated and output is consistent.

- **Implement Quality Scoring and Field Masking** (Priority: medium)
  Score leads based on data completeness and optimize API usage with field masking.
  
  *Details:* Develop a scoring algorithm that evaluates completeness of contact info and other key fields. Adjust API requests to only fetch necessary fields. Store qualityScore in ProspectedLead.

- **Add Rate Limiting, Exponential Backoff, and Cost Optimization** (Priority: medium)
  Ensure compliance with Google API rate limits, implement exponential backoff, and monitor API usage for cost control.
  
  *Details:* Implement request throttling and exponential backoff for failed requests. Track API usage and enforce 10,000 calls/month limit. Log and alert when nearing quota.

- **Integrate Apify Google Maps Actors for High-Volume Fallback** (Priority: medium)
  Automatically switch to Apify actors when Google Places API quota is exceeded or for large-scale operations.
  
  *Details:* Detect when API quota is reached. Trigger Apify actor runs via their API. Parse and standardize Apify results into ProspectedLead format. Ensure seamless fallback and recovery.

- **Implement Scheduler and Monitoring** (Priority: medium)
  Automate discovery cycles using Vercel cron jobs and set up performance monitoring.
  
  *Details:* Configure Vercel cron jobs based on user schedule. Monitor uptime, API success rate, processing speed, and deduplication rate. Log metrics for analysis and alert on anomalies.


## Installation

```bash
npm install
```

## Configuration

Configuration is handled through the `config/default.json` file and environment variables.

### Environment Variables

- `LOG_LEVEL` - Set logging level (debug, info, warn, error)
- `TIMEOUT` - Set operation timeout in milliseconds
- `NODE_ENV` - Set environment (development, production)

### Config File

See `config/default.json` for detailed configuration options.

## Usage

### Programmatic Usage

```javascript
const { ProspectorAgent } = require('./main');

async function example() {
  const agent = new ProspectorAgent({
    logLevel: 'info',
    timeout: 30000
  });
  
  await agent.initialize();
  const result = await agent.process({ 
    // Your input data here
  });
  
  console.log('Result:', result);
  await agent.cleanup();
}

example().catch(console.error);
```

### Direct Execution

```bash
node main.js
```

### Using the main function

```javascript
const { main } = require('./main');

main({
  input: { /* your data */ },
  logLevel: 'info'
}).then(result => {
  console.log('Success:', result);
}).catch(error => {
  console.error('Error:', error);
});
```

## API Reference

### ProspectorAgent Class

#### Constructor

```javascript
new ProspectorAgent(config)
```

- `config` (Object) - Configuration options
  - `logLevel` (string) - Logging level (default: 'info')
  - `timeout` (number) - Operation timeout in ms (default: 30000)

#### Methods

##### `initialize()`

Initialize the agent and prepare for processing.

**Returns:** `Promise<void>`

##### `process(input)`

Process the provided input data.

**Parameters:**
- `input` (Object) - Data to process

**Returns:** `Promise<Object>` - Processing result

##### `cleanup()`

Clean up resources and connections.

**Returns:** `Promise<void>`

##### `getStatus()`

Get current agent status information.

**Returns:** `Object` - Status information

## Development

### Project Structure

```
prospector/
├── main.js              # Main agent implementation
├── config/
│   └── default.json     # Configuration file
├── templates/           # Template files (if applicable)
├── tests/               # Test files
└── README.md           # This file
```

### Testing

```bash
npm test
```

### Linting

```bash
npm run lint
```

## Error Handling

The agent implements comprehensive error handling:

- All operations are wrapped in try-catch blocks
- Errors are logged with appropriate severity levels
- Failed operations throw descriptive error messages
- Cleanup is performed even when errors occur

## Logging

The agent uses `chalk` for colored console output:

- 🚀 Blue: Initialization and process start messages
- ✅ Green: Success messages
- ❌ Red: Error messages
- 🔄 Blue: Processing status updates
- 🧹 Blue: Cleanup messages

## Contributing

1. Follow the existing code style and patterns
2. Add tests for new functionality
3. Update documentation as needed
4. Ensure all tests pass before submitting changes

## License

ISC

---

*Generated by Scaffold Generator on 2025-07-25 11:31:02*