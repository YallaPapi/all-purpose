# Master Meta-Agent Guide
*The definitive autonomous workflow: PRD IN → FUNCTIONAL PROJECT OUT*

## Critical Success Pattern
**This system ALREADY WORKS** - proved by YouTube/GitHub project generation.  
**Goal**: PRD in → Functional project out, ZERO manual intervention required.

---

## The One-Command Solution

### Input: PRD Document
### Output: Complete Functional Project  
### Method: Infrastructure Orchestrator Agent (IOA) Coordination

```bash
# Single command that does everything:
node src/meta-agents/infra-orchestrator/dist/main.js orchestrate --project-root ./generated --enable-investigation
```

---

## Master Workflow (Autonomous)

### Phase 1: Initialization
1. **TaskMaster Parse**: `task-master parse-prd --input="project-prd.md"`
2. **IOA Startup**: Initialize Infrastructure Orchestrator Agent
3. **Context Loading**: Load all project context and patterns

### Phase 2: Agent Coordination (Automatic)
1. **PRD-Parser** → Parse requirements into structured tasks
2. **All-Purpose-Pattern** → Apply zero-limitation methodology  
3. **Scaffold-Generator** → Create complete project structure
4. **Template-Engine** → Generate all boilerplate code
5. **Parameter-Flow** → Configure data architecture
6. **Vercel-Architecture** → Setup deployment infrastructure
7. **Five-Document-Framework** → Generate documentation
8. **Thirty-Minute-Rule** → Validate task complexity

### Phase 3: Output Validation (Automatic)
1. **File Generation**: All project files created
2. **Code Compilation**: No syntax errors
3. **Test Suite**: All tests pass
4. **Documentation**: Complete setup guides
5. **Deployment**: Vercel-ready configuration

---

## Success Criteria Checklist

✅ **Input**: Single PRD markdown file  
✅ **Processing**: Zero manual intervention  
✅ **Output**: Functional project in `/generated` directory  
✅ **Quality**: Tests pass, deploys successfully  
✅ **Documentation**: Complete README and setup guides  

---

## Emergency Recovery Commands

If anything fails, use these proven working commands:

```bash
# 1. Reset and restart
cd C:\Users\Stuart\Desktop\Projects\allpurp
rm -rf generated/*

# 2. Parse PRD with TaskMaster
task-master parse-prd --input="prospector-agent-prd.md" --research

# 3. Direct IOA execution
cd src/meta-agents/infra-orchestrator
npm run build
node dist/main.js orchestrate --project-root ../../../generated --enable-investigation

# 4. Verify output
ls -la ../../../generated/
```

---

## Proven Working Example

**YouTube/GitHub Project** was successfully generated using this exact process:
- PRD → TaskMaster → IOA → Complete functional system
- No manual intervention
- All integrations working
- Full documentation generated

**Replication**: Follow the same pattern for Prospector Agent.

---

## Key Insights

1. **Don't rebuild** - Use existing working system
2. **Don't overcomplicate** - IOA handles coordination automatically  
3. **Don't manual intervene** - Let agents work autonomously
4. **Trust the process** - It already proved it works

---

## Final Command Sequence (Copy-Paste)

```bash
# Go to project root
cd C:\Users\Stuart\Desktop\Projects\allpurp

# Parse PRD (already done)
# task-master parse-prd --input="prospector-agent-prd.md" --research

# Execute Infrastructure Orchestrator directly
cd src/meta-agents/infra-orchestrator
npm install && npm run build
node dist/main.js orchestrate --project-root ../../../generated --enable-investigation --project-name prospector-agent

# Check results
ls -la ../../../generated/prospector-agent/
```

This is the proven, working approach. No API routes, no compilation errors, no manual coordination - just pure meta-agent autonomy.