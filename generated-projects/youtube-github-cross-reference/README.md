# youtube-github-cross-reference

Agent for youtube-github-cross-reference functionality

## Overview

This agent was automatically generated by the Scaffold Generator on 2025-07-23 23:42:58 based on PRD-Parser output.

## Features

### Planned Implementation Tasks

- **Set up Next.js project structure** (Priority: high)
  Initialize Next.js 15+ project with TypeScript and required dependencies

- **Implement YouTube scraper service** (Priority: high)
  Build service to search and extract YouTube video data and transcripts

- **Create GitHub repository analyzer** (Priority: high)
  Service to index and analyze GitHub repositories

- **Build semantic search engine** (Priority: high)
  Implement embedding generation and similarity search using OpenAI and Upstash Vector

- **Develop web interface** (Priority: medium)
  Create React components for search, display, and exploration

- **Implement API endpoints** (Priority: medium)
  Build Next.js API routes for all system functionality

- **Build cross-reference matching** (Priority: medium)
  Algorithm to match tutorials with relevant repositories

- **Testing and deployment** (Priority: low)
  Comprehensive testing and production deployment setup


## Installation

```bash
npm install
```

## Configuration

Configuration is handled through the `config/default.json` file and environment variables.

### Environment Variables

- `LOG_LEVEL` - Set logging level (debug, info, warn, error)
- `TIMEOUT` - Set operation timeout in milliseconds
- `NODE_ENV` - Set environment (development, production)

### Config File

See `config/default.json` for detailed configuration options.

## Usage

### Programmatic Usage

```javascript
const { Youtube-Github-Cross-ReferenceAgent } = require('./main');

async function example() {
  const agent = new Youtube-Github-Cross-ReferenceAgent({
    logLevel: 'info',
    timeout: 30000
  });
  
  await agent.initialize();
  const result = await agent.process({ 
    // Your input data here
  });
  
  console.log('Result:', result);
  await agent.cleanup();
}

example().catch(console.error);
```

### Direct Execution

```bash
node main.js
```

### Using the main function

```javascript
const { main } = require('./main');

main({
  input: { /* your data */ },
  logLevel: 'info'
}).then(result => {
  console.log('Success:', result);
}).catch(error => {
  console.error('Error:', error);
});
```

## API Reference

### Youtube-Github-Cross-ReferenceAgent Class

#### Constructor

```javascript
new Youtube-Github-Cross-ReferenceAgent(config)
```

- `config` (Object) - Configuration options
  - `logLevel` (string) - Logging level (default: 'info')
  - `timeout` (number) - Operation timeout in ms (default: 30000)

#### Methods

##### `initialize()`

Initialize the agent and prepare for processing.

**Returns:** `Promise<void>`

##### `process(input)`

Process the provided input data.

**Parameters:**
- `input` (Object) - Data to process

**Returns:** `Promise<Object>` - Processing result

##### `cleanup()`

Clean up resources and connections.

**Returns:** `Promise<void>`

##### `getStatus()`

Get current agent status information.

**Returns:** `Object` - Status information

## Development

### Project Structure

```
youtube-github-cross-reference/
├── main.js              # Main agent implementation
├── config/
│   └── default.json     # Configuration file
├── templates/           # Template files (if applicable)
├── tests/               # Test files
└── README.md           # This file
```

### Testing

```bash
npm test
```

### Linting

```bash
npm run lint
```

## Error Handling

The agent implements comprehensive error handling:

- All operations are wrapped in try-catch blocks
- Errors are logged with appropriate severity levels
- Failed operations throw descriptive error messages
- Cleanup is performed even when errors occur

## Logging

The agent uses `chalk` for colored console output:

- 🚀 Blue: Initialization and process start messages
- ✅ Green: Success messages
- ❌ Red: Error messages
- 🔄 Blue: Processing status updates
- 🧹 Blue: Cleanup messages

## Contributing

1. Follow the existing code style and patterns
2. Add tests for new functionality
3. Update documentation as needed
4. Ensure all tests pass before submitting changes

## License

ISC

---

*Generated by Scaffold Generator on 2025-07-23 23:42:58*