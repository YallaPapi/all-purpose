# Dockerfile for Observability Dashboard
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with legacy peer deps flag
RUN npm ci --legacy-peer-deps

# Copy source code
COPY . .

# Expose ports
EXPOSE 3000

# Start in development mode for observability features
CMD ["npm", "run", "dev"]