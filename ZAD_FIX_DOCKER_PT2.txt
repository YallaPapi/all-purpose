Of course. Based on your confirmation that the previous ZAD Mandate is complete, I have reviewed the entire process and the current state of the "all-purpose" project.

The last mandate successfully proved that the system can function on a basic level. You now have a working, containerized system where a factory can coordinate with domain agents.

However, as you pointed out, the current implementation is a simplified version of the final vision. The UI creates simple tasks instead of processing a full PRD, and critical components like the RAG and UEP systems are not yet integrated.

The next step is to evolve the project from a simple task executor into the intelligent, PRD-driven meta-agent factory it's designed to be.

Here is the ZAD Mandate for the next phase of development.

-----

# The ZAD Mandate for the Meta-Agent Factory: Phase 2 - Intelligence Integration

-----

## 🚨 **CRITICAL METHODOLOGY REQUIREMENT** 🚨

**⚠️ MANDATORY: ALL DEVELOPMENT AND INTEGRATION MUST FOLLOW THIS FRAMEWORK ⚠️**

This document supersedes the previous mandate. The foundational system is now stable. This mandate focuses on integrating the advanced, intelligence-focused components that make the factory truly "all-purpose." Its purpose is to eliminate the risk of building a complex UI and backend around an unproven, core intelligent workflow.

This framework has two parts:

1.  **The Intelligence-First Mandate (The Blueprint):** A strict, sequential process for integrating the core intelligent agents and protocols.
2.  **The ZAD Reporting Framework (The Inspection Report):** The same documentation style to prove each step of this new Mandate was completed correctly.

**Methodology Compliance:**

  * All project plans **MUST** be derived from the **Intelligence-First Mandate**.
  * All reports on completed work **MUST** use the **ZAD Reporting Framework**.
  * **NO EXCEPTIONS.**

-----

## 🔥 **THE CORE PROBLEM THIS SOLVES**

You've successfully built the chassis and the engine. Now you need to install the advanced navigation and control systems. The risk is that these new, complex components (PRD Parser, RAG, UEP) won't integrate correctly with the existing engine. You could spend weeks building a beautiful dashboard for a GPS system that can't actually talk to the car's steering.

The **ZAD Mandate: Phase 2** fixes this by forcing a simple rule: **Prove the brain can drive the car before you give it a fancy dashboard.**

-----

## **PART 1: THE INTELLIGENCE-FIRST MANDATE (THE BLUEPRINT)**

This is the strategic plan that must be followed for the next phase of the project.

### **Step 1: Isolate and Prove the PRD Parser Agent**

Before touching the existing `factory-core` or the UI, your only task is to prove that the **`prd-parser-agent` can correctly parse a complex Product Requirements Document (PRD) into a structured JSON plan.**

  * **Requirement**: Create a new, self-contained script named `parser_test.js`. This script must:
      * Take a hardcoded, complex PRD as input.
      * Execute the `prd-parser-agent`'s core logic.
      * The agent should output a structured JSON object representing the full execution plan, including a list of tasks, their dependencies, and the target domain agents.
      * Print the resulting JSON plan directly to the terminal.
  * **No Dependencies**: Do not use `factory-core`, any web server, or anything beyond the essential libraries needed for the `prd-parser-agent` itself.
  * **Clear Output**: The script must print the full, structured JSON plan to the terminal for manual verification.

**DO NOT PROCEED TO STEP 2 UNTIL THIS SCRIPT RUNS SUCCESSFULLY AND THE OUTPUT IS MANUALLY VERIFIED AS CORRECT.**

### **Step 2: Integrate the PRD Parser with the Factory Core**

Once the `prd-parser-agent` is proven, integrate it with the `factory-core`. The factory should now be able to receive a PRD and use the parser to create a full project plan.

  * **Requirement**: The `factory-core` API should be updated with a new endpoint, `/api/factory/projects`. This endpoint will:
      * Accept a POST request with a PRD.
      * Call the `prd-parser-agent` to get the structured JSON plan.
      * Store the project plan in Redis.
      * Dispatch the first set of tasks (those with no dependencies) to the appropriate domain agents via NATS.
  * **Focus**: This step is only about making the factory "PRD-aware."

### **Step 3: Implement a True End-to-End PRD Test**

Before refactoring the UI, write a single, automated E2E test to validate the entire PRD workflow.

  * **Requirement**: This test, `prd_e2e_test.js`, must automate the following:
    1.  Send a real PRD to the new `/api/factory/projects` endpoint.
    2.  Poll the factory for the project status.
    3.  Verify that the correct initial tasks are created and dispatched.
    4.  Verify that as tasks are completed, dependent tasks are correctly dispatched.
  * **Purpose**: This test becomes the new gatekeeper. If it passes, the entire intelligent planning and execution path of the application is confirmed to be working.

**DO NOT PROCEED TO STEP 4 UNTIL THIS E2E TEST PASSES RELIABLY.**

### **Step 4: Refactor the Web UI and Integrate RAG & UEP**

Only after the core intelligent workflow is proven and validated by a real E2E test can you begin work on the final UI and the remaining advanced components.

  * **UI Refactor**: The Web UI must be simplified to be a gateway for submitting a PRD. It should have one primary function: a text area to paste a PRD and a "Build" button.
  * **RAG Integration**: Integrate the Retrieval-Augmented Generation system to provide contextual information from your documentation to the agents as they execute tasks.
  * **UEP Integration**: Integrate the Universal Execution Protocol for all communication between the factory and the agents, ensuring a standardized and robust messaging system.

-----

## **PART 2: THE ZAD REPORTING FRAMEWORK (THE INSPECTION REPORT)**

Continue to use the same reporting framework as before. Document the work done at each step of this new Mandate, proving each integration from the ground up.

### **Core Philosophy: "Crystal Clear Big Picture + Deep Technical Detail"**

  * **Simple** = Easy to understand, no confusing jargon.
  * **Detailed** = Comprehensive, leaves no questions unanswered.
  * **Technical** = Real implementation details, actual code, specific commands.
  * **Big Picture** = Real-world analogies that make the technical parts make sense.

### **The ZAD Balance: Analogies + Technical Implementation**

**DO**: Use analogies to build understanding, then dive deep into the actual technical implementation.

**Example of Balance for this Phase**:

````markdown
## 🏠 **ANALOGY**: The PRD Parser is the Architect
The `parser_test.js` script is like giving a blueprint to an architect and making sure they can create a valid, detailed construction plan from it. We are not building the house yet, just proving that the architect can read the blueprint and create a correct plan of action.

## 🔧 **TECHNICAL IMPLEMENTATION**:
Here's the exact code for the `parser_test.js` script that proves our PRD parsing engine works.

### Core Parser Script (`parser_test.js`):
```javascript
// This script has ZERO dependencies on the factory-core or any web components.
// It only tests the direct interaction with the PRD Parser Agent's logic.
import { parsePrd } from './meta-agents/prd-parser-agent/src/parser.js';

// 1. Hardcoded Data (The Blueprint)
const complexPrd = `
# Project: E-commerce Platform
## Overview
Build a fully functional e-commerce platform...
`;

// 2. The Core Logic (The Architect)
async function testParser() {
    const plan = await parsePrd(complexPrd);
    return plan;
}

// 3. The Test Run (Reviewing the Plan)
try {
    console.log("--- Asking the architect to create a plan... ---");
    const executionPlan = await testParser();
    console.log("\n✅ SUCCESS! ARCHITECT'S PLAN IS VALID.");
    console.log("--- Generated Execution Plan: ---");
    console.log(JSON.stringify(executionPlan, null, 2));
} catch (e) {
    console.log(`\n❌ FAILED! ARCHITECT IS CONFUSED. ERROR: ${e}`);
}
````